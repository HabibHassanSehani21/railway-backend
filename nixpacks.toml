[project]
name = "pdf-tools-api"

[phases.setup]
# System packages Pillow needs to compile
packages = [
  "python311",
  "python311Packages.pip",
  "python311Packages.setuptools",
  "python311Packages.wheel",
  "zlib",
  "zlib-dev",
  "libjpeg-turbo",
  "libjpeg-turbo-dev",
  "libtiff",
  "libtiff-dev",
  "freetype",
  "freetype-dev",
  "libwebp",
  "libwebp-dev",
  "pkg-config",
  "gcc",
  "make"
]

[phases.build]
# Upgrade pip & install Python packages
commands = [
  "python -m pip install --upgrade pip setuptools wheel",
  "python -m pip install --no-cache-dir -r requirements.txt"
]

[phases.start]
commands = [
  "uvicorn main:app --host 0.0.0.0 --port $PORT"
]
